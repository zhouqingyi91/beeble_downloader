{
  "name": "beeble-downloader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install-browsers": "playwright install chromium",
    "run": "node src/cli.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "^1.57.0"
  }
}
